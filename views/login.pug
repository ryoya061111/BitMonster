extends layout

block content
  .wrapper
    p.main-title
    form.board-form(action="/login" method="post")
      span.label Eメール
      input.input(type="email" name="email" required="")
      br
      br
      span.label パスワード
      input.input(type="password" name="password" required="")
      br
      br
      button.submit(type="submit") ログイン
